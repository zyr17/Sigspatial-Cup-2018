#include <bits/stdc++.h>
#include "parse.h"

std::vector<std::string> solve(std::vector<std::string> &points, std::vector<parse::edge> &edges, std::vector<std::string> &starts, std::vector<std::string> &ends){
	
}

int main(){
	std::vector<std::string> points, starts, ends;
	std::vector<parse::edge> edges;
	parse::parse(poitns, edges, starts, ends);
	auto res = solve(points, edges, starts, ends);
}